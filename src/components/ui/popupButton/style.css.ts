import { style } from "@vanilla-extract/css"

const styles = {
    button: style({
        width: 200,
        height: 50,
        color: "white",
        fontSize: 24,
    }),
}

export default styles
