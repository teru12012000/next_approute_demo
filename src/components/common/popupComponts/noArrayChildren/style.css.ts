import { style } from "@vanilla-extract/css"

const styles = {
    containar: style({
        height: 450,
        width: "100%",
        display: "flex",
        textAlign: "center",
        alignItems: "center",
    }),
}

export default styles
