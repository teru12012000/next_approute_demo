import { style } from "@vanilla-extract/css"

const styles = {
    containar: style({
        width: "100%",
        height: 100,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
    }),
}

export default styles
