import { style } from "@vanilla-extract/css"

const styles = {
    containar: style({
        height: 450,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
    }),
}

export default styles
