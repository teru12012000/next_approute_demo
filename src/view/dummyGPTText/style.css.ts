import { style } from "@vanilla-extract/css"

const styles = {
    containar: style({
        width: "100vw",
        height: "100vh",
        backgroundColor: "white",
    }),
    mainContent: style({
        display: "flex",
    }),
}

export default styles
