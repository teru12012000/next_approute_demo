import { style } from "@vanilla-extract/css"

const styles = {
    box: style({
        width: 360,
        height: 90,
        border: "1px solid black",
        borderRadius: 5,
    }),
}

export default styles
